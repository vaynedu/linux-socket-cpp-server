1. 在之前的简单聊天室的基础，改用epoll版简单的聊天室，采用边缘触发

2.获取系统打开的文件描述符的数量  cat /proc/sys/fs/file-nr

3. ulimit -a 查看资源的限制情况 ulimit -n  1024 更改文件描述符的限制

4. rlimit一个进程在执行过程中资源的限制，文件描述符数量的限制。可以用
getrlimit 和 setrlimit












